#pragma once

#include <string>
#include <vector>
#include <filesystem>

#include <format>
#include <iostream>

using string = std::string;
using string_list = std::vector<string>;

namespace fs = std::filesystem;